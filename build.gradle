subprojects {
    apply plugin: 'groovy'
    repositories {
        maven {
            url "https://dl.bintray.com/sandstorm/maven"
        }
        mavenCentral()
    }
}
task wrapper(type: Wrapper) {
    gradleVersion = '2.2'
}
